<!DOCTYPE html>
<html lang="en-US">
<head>
	<meta charset="utf-8">
	<title>Roblox Lua API Reference</title>
	<link href="{{link "res" "main.css"}}" rel="stylesheet" type="text/css" />
	<style>
		main article ul {
			margin-left  : 0;
			padding-left : var(--indent);
		}
		main article:not(#types) ul {
			list-style: none;
		}
		main {
			--indent     : calc(var(--icon-size) + var(--icon-spacing));
		 	--list-depth : 5;
		 	--name-width : 24ch;
		 	--grid-width : calc(
		 		var(--list-depth)*var(--indent) +
		 		var(--icon-size) +
		 		var(--icon-spacing) +
		 		var(--name-width)
		 	);
			display               : grid;
			grid-template-columns : repeat(auto-fit, minmax(var(--grid-width), 1fr));
			justify-items         : left;
			grid-template-rows    : auto 1fr;
		}
		#classes {
			grid-row-end: span 2;
		}
	</style>
</head>
<body>
	<header>
		<h1>Roblox Lua API Reference</h1>
	</header>
	<p>This site is under construction.</p>
	<nav>
		<ul>
			<li><a href="{{link "updates"}}#{{.Latest.Info.Hash}}">Latest API updates</a></li>
		</ul>
	</nav>
	<main>
		<article id="classes">
			<header>
				<h2>Classes</h2>
			</header>
			<ul id="class-list">
				{{- define "tree" -}}
				{{- range . }}
				<li><a class="element-link" href="{{link "class" .}}">{{icon "class" .}}{{.}}</a>
				{{- if subclasses . }}
				<ul>
				{{- template "tree" (subclasses .) }}
				</ul>
				{{- end -}}
				</li>
				{{- end -}}
				{{- end -}}
				{{- template "tree" .TreeRoots }}
			</ul>
		</article>
		<article id="enums">
			<header>
				<h2>Enums</h2>
			</header>
			<ul id="enum-list">
				{{- range .Latest.API.Enums }}
				<li><a class="element-link" href="{{link "enum" .Name}}">{{icon .}}{{.Name}}</a></li>
				{{- end }}
			</ul>
		</article>
		<article id="types">
			<header>
				<h2>Types</h2>
			</header>
			<ul id="type-list">
			{{- range .TypeList }}
				<li>{{.Name}}
					<ul>
					{{- range .Types }}
						<li>{{template "value" .}}</li>
					{{- end }}
					</ul>
				</li>
			{{- end }}
			</ul>
		</article>
	</main>
</body>
</html>
